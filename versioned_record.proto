syntax = "proto3";

option go_package = "github.com/zitadel/logging";

import "records/v1/access.proto";

message VersionedRecord {
  oneof record {
    AccessRecord access_v1 = 1;
    // AccessRecord access_v2 = 2;
  }
}